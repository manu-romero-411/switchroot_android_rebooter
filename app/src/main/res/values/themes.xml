<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base: Material3 con DayNight. DynamicColors.applyToActivityIfAvailable()
         se encarga en tiempo de ejecución de aplicar la paleta "Material You" del
         sistema en Android 12+ (API 31+); en versiones anteriores cae a la paleta
         estática por defecto de Material3. -->
    <style name="Theme.SwitchRebootManager" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Variante estilo popup flotante: fondo transparente, dimm sobre la pantalla
         anterior, y comportamiento de diálogo para que no se vea pantalla negra. -->
    <style name="Theme.SwitchRebootManager.Fullscreen" parent="Theme.Material3.DayNight.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <!-- Forzamos a que ignore los anchos mínimos de diálogo estándar -->
        <item name="android:windowMinWidthMajor">0%</item>
        <item name="android:windowMinWidthMinor">0%</item>
    </style>

</resources>
